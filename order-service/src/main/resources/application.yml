server:
  port: 0

spring:
  application:
    name: order-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://ec2-3-35-27-135.ap-northeast-2.compute.amazonaws.com:3306/MTBX
    username: mtbx
    password: Qwer1234!@#
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
greeting:
  message: Welcome Ecommerce
logging:
  level:
    com.example.orderservice: DEBUG

